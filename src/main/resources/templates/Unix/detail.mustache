{{>header}}

<div style="max-width: 800px; margin: 40px auto; font-family: sans-serif;">
    <h2 style="text-align: center;">📘 {{chapter.title}}</h2>

    <div style="background: #f9f9f9; padding: 20px; border-radius: 10px; margin-top: 20px;">
        <p style="color: #777; font-size: 14px;">작성일: {{chapter.createdAt}}</p>
        <hr>
        <div style="white-space: pre-line; font-size: 16px; line-height: 1.6; color: #333;">
            {{chapter.content}}
        </div>
    </div>

    <!-- ✅ 수정/삭제 버튼 -->
    <div style="margin-top: 30px; text-align: right; display: flex; justify-content: flex-end; gap: 10px;">
        <a href="/subjects/unix/{{chapter.id}}/edit"
           style="padding: 6px 14px; background: #f1c40f; color: white; border-radius: 5px; text-decoration: none;">
            ✏ 수정
        </a>

        <form action="/subjects/unix/{{chapter.id}}/delete" method="post" onsubmit="return confirm('정말 삭제하시겠습니까?');">
            {{#csrfToken}}
                <input type="hidden" name="{{csrfParameterName}}" value="{{csrfToken}}">
            {{/csrfToken}}
            <button type="submit"
                    style="padding: 6px 14px; background: #e74c3c; color: white; border: none; border-radius: 5px; cursor: pointer;">
                🗑 삭제
            </button>
        </form>
    </div>

    <!-- ✅ 목록으로 돌아가기 -->
    <div style="margin-top: 30px; text-align: center;">
        <a href="/subjects/unix" style="text-decoration: none; color: #2980b9;">← 목록으로 돌아가기</a>
    </div>
</div>

{{>footer}}
