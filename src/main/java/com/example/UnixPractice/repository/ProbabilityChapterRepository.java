package com.example.UnixPractice.repository;

import com.example.UnixPractice.entity.ProbabilityChapter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProbabilityChapterRepository extends JpaRepository<ProbabilityChapter, Long> {
}
