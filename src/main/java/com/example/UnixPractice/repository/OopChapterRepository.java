package com.example.UnixPractice.repository;

import com.example.UnixPractice.entity.OopChapter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OopChapterRepository extends JpaRepository<OopChapter, Long> {
}
